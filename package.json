{
  "name": "klaviyo-list-cleaner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute list-cleaner-db --file=schemas/schema.sql",
    "db:init:local": "wrangler d1 execute list-cleaner-db --local --file=schemas/schema.sql",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
